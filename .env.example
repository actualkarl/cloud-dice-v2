# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (for development)
# In production, CORS will be automatically configured for the same origin
CORS_ORIGIN=http://localhost:5173

# Optional: Custom configuration
# MAX_ROOMS=1000
# ROOM_CLEANUP_INTERVAL=300000
# INACTIVE_ROOM_THRESHOLD=1800000

# Railway automatically provides:
# - PORT (the port your app should listen on)
# - RAILWAY_ENVIRONMENT (production, staging, etc.)
# - Various other Railway-specific environment variables

# For local development, copy this file to .env and modify as needed
# The app will work with default values if no .env file is present